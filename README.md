To run this app, run the following commands in this project's directory via the terminal:

- composer update
- npm install
- php artisan filament:assets
- php artisan key:generate
- php artisan migrate
- rename .env.example to .env
- npm run dev
